import { colors, fonts, grid, mixins, functions } from '../abstracts';
import { utilities, animations } from '../base';

export const defaultTheme = {
  ...colors,
  ...grid,
  ...fonts,
  ...functions,
  animations,
  mixins,
  utilities
};

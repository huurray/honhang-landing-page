import colors from './colors'; // app colors
import media from './media'; // media query
import utils from './utils'; // 유용한 CSS 메서드
import mixins from './mixins'; // 공통으로 자주 사용되는 CSS
import animations from './animations'; // 애니메이션

export default {
  colors,
  media,
  utils,
  mixins,
  ani: animations
};
